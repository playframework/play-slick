# Configuration

application.name=computer-database

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="E27D^[_<Lpt0vjad]de;3;tx3gpRmG4ByofnahOIo9gbsMWut1w3xg[>9W"

# Database configuration
# ~~~~~ 
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
slick.db.default.driver="slick.driver.H2Driver$"
slick.db.default.db.driver="org.h2.Driver"
slick.db.default.db.url="jdbc:h2:mem:play"
slick.db.default.db.user=sa
slick.db.default.db.password=""

# Slick Evolutions
# ~~~~~
slick.evolutions.default="dao.CompaniesDAO,dao.ComputersDAO"

# Assets configuration
# ~~~~~
"assets.cache./public/stylesheets/bootstrap.min.css"="max-age=3600"

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory .

# Root logger:
logger.root=ERROR

# Logger used by the framework:
logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG


pullRequests.frequency = "@monthly"

commits.message = "[6.2.x] ${artifactName} ${nextVersion} (was ${currentVersion})"

pullRequests.grouping = [
  { name = "patches", "title" = "[6.2.x] Patch updates", "filter" = [{"version" = "patch"}] }
]

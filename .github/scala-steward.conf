pullRequests.frequency = "@monthly"

commits.message = "[6.1.x] ${artifactName} ${nextVersion} (was ${currentVersion})"

pullRequests.grouping = [
  { name = "patches", "title" = "[6.1.x] Patch updates", "filter" = [{"version" = "patch"}] }
]
